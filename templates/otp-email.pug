doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title OTP Verification |LMS Platform
    style.
      /* Google Fonts */
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap');

      /* CSS Variables for easy theming */
      :root {
        --primary-gradient: linear-gradient(135deg, #667eea, #764ba2);
        --text-color: #ffffff;
        --highlight-color: #ffd700;
        --container-bg: rgba(255, 255, 255, 0.1);
        --font-family: 'Inter', sans-serif;
      }

      /* Reset & base styles */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        background: var(--primary-gradient);
        font-family: var(--font-family), sans-serif;
        color: var(--text-color);
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        padding: 1rem;
      }
      .card {
        background: var(--container-bg);
        padding: 2.5rem 3rem;
        border-radius: 1rem;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.25);
        text-align: center;
        max-width: 420px;
        width: 100%;
        backdrop-filter: blur(10px);
        animation: fadeIn 0.6s ease-out;
      }
      @keyframes fadeIn {
        from { opacity: 0; transform: translateY(15px); }
        to { opacity: 1; transform: translateY(0); }
      }
      header img {
        width: 60px;
        margin-bottom: 1rem;
      }
      h1 {
        font-size: 1.75rem;
        font-weight: 700;
        margin-bottom: 1rem;
        letter-spacing: 0.05em;
      }
      p {
        font-size: 1rem;
        margin-bottom: 1rem;
        opacity: 0.85;
      }
      h2 {
        font-size: 3rem;
        font-weight: 700;
        letter-spacing: 0.15em;
        margin: 1rem 0 1.5rem;
        color: var(--highlight-color);
        text-shadow: 0 0 8px #ffd700aa;
      }
      footer {
        font-size: 0.85rem;
        opacity: 0.6;
        margin-top: 1.5rem;
      }
      @media (max-width: 480px) {
        .card {
          padding: 2rem;
        }
        h2 {
          font-size: 2.5rem;
        }
      }
  body
    main.card
      header
        //- Optional brand logo
        img(src="https://via.placeholder.com/60" alt="LMS Logo")
        h1 Email Verification Code
      section
        p Your OTP is:
        h2 #{otp}
        p Please use this code to verify your account.
      footer
        p This code will expire in 5 minutes. If you didn’t request this, please ignore this email.
